{
  "name": "neugreen-site",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio marketing Neugreen — precompilación JSX con esbuild (sin Babel en el navegador)",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
